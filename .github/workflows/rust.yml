name: CI

on:
  push:
    branches: [main]
  pull_request:
    branches: [main]

concurrency:
  group: ${{ github.workflow }}-${{ github.ref }}
  cancel-in-progress: true

env:
  CARGO_TERM_COLOR: always

jobs:
  test:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v6
      - uses: dtolnay/rust-toolchain@stable
      - uses: Swatinem/rust-cache@v2
      - run: cargo test --all-features

  # TODO: Enable e2e tests once sample test data is added
  # Requires: test user (test_user/test_password_123) and sample photos in playwright/fixtures/images/
  # e2e:
  #   runs-on: ubuntu-latest
  #   steps:
  #     - uses: actions/checkout@v6
  #     - uses: dtolnay/rust-toolchain@stable
  #     - uses: Swatinem/rust-cache@v2
  #     - uses: actions/setup-node@v4
  #       with:
  #         node-version: 20
  #         cache: npm
  #     - run: npm ci
  #     - run: npx playwright install --with-deps chromium
  #     - name: Setup test data
  #       run: |
  #         mkdir -p /tmp/familyphotos-test
  #         ./target/release/familyphotos users create -u test_user -n "Test User" -p test_password_123
  #         cp -r playwright/fixtures/images/* /tmp/familyphotos-test/
  #       env:
  #         STORAGE_PATH: /tmp/familyphotos-test
  #         DATABASE_URL: /tmp/familyphotos-test/.familyphotos.db
  #     - name: Build and start server
  #       run: |
  #         cargo build --release
  #         ./target/release/familyphotos &
  #         sleep 3
  #       env:
  #         SERVER_PORT: 3000
  #         STORAGE_PATH: /tmp/familyphotos-test
  #     - run: npm run test